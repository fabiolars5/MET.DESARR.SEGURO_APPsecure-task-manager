services:
  secure-task-manager:
    build: .
    container_name: secure-task-manager
    ports:
      - "3000:3000"
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=change-me-in-production
      - DATABASE_PATH=/app/data/database.db
    volumes:
      - ./app/data:/app/data